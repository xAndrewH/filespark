// Empty stub for Node.js built-ins when bundled for browser.
// Emscripten modules guard all uses with ENVIRONMENT_IS_NODE checks,
// so these stubs are imported but never actually called at runtime.
module.exports = {};
